{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "rm -rf node_modules/.cache .vite .npm && npm ci && npm run build"
  },
  "deploy": {
    "startCommand": "npm start"
  }
}