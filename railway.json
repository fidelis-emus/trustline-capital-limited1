{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "buildCommand": "npm ci && npm run build"
  },
  "deploy": {
    "startCommand": "tsx server.ts"
  }
}