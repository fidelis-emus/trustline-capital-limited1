{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npx node@20 npm install && npm run build"
  },
  "deploy": {
    "startCommand": "npm start"
  }
}